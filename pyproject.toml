[project]
name = "grn-inference-diagnosis"
version = "0.1.0"
description = "Controlled diagnostics for gene regulatory network inference methods on simulated single-cell data."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.9",
    "networkx>=3.6",
    "numpy>=2.3.5",
    "pandas>=2.3.3",
    "scikit-learn>=1.8.0",
    "scipy>=1.16.3",
]
